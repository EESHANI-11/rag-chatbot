# existing lines ...
streamlit
chromadb
pymupdf
sentence-transformers
torch
rank-bm25

groq
openai>=1.30.0

pandas
tqdm
python-dotenv

# pin these to avoid the PyArrow breakage
pyarrow<17
datasets==2.18.0

# optional (only if you actually use the Evaluation tab with RAGAS)
ragas
langchain-openai
